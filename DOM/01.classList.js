const h1 = document.querySelector("h1");
console.log(h1.classList);

// add the classList
h1.classList.add("added");

// remove the classlist
h1.classList.remove("added");

// Toggle the classList
h1.classList.toggle("added");
