// What is arrays?
// arrays are data structure that allow you to store a collection of data at once. arrays are something called 0 index based.

// const array = [1, 2, 3, 4, 5, 6, 7, 8];
// console.log(array[3]);
// Return array item at index 3 which is 4

// Nested array

// let array = ["ali", "ahmed", [2, 3, 4], "Qaisar"];

// console.log(array[2][0]);

// inside the array the item at index 2 and inside the item the element at index 0 which is 2.

// Methods of array.
// push
let fruit = ["apple", "banana"];
// console.log(fruit.push("graps"));
// console.log(fruit);
// push the item at the end of array.

// pop
// console.log(fruit.pop());
// console.log(fruit);
// remove item at the end of array

// shift
// console.log(fruit.shift());
// reomve item at the start of array

// Unshift
// console.log(fruit.unshift("graps"));
// console.log(fruit);
// Add item at the start of array

// concat
const moreFruit = ["graps", "cherry", "orange"];
// const totalFruit = fruit.concat(moreFruit);
// console.log(totalFruit);

// include
// console.log(moreFruit.includes("cherry"));

// join
// console.log(moreFruit.join("_"));

// reverse
// console.log(moreFruit.reverse());

// slice
// console.log(moreFruit.slice(0, 2));

// sort
// const num = [1, 2, 5, 3, 2, 5, 7, 5, 8, 0];
// console.log(num.sort());
